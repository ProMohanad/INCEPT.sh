"""Natural language templates for all 52 INCEPT intents.

Each intent maps to 10-20 template strings containing ``{slot_name}``
placeholders that correspond to the slot names defined in the param
schemas.  Templates are designed with diverse phrasing styles
(imperative, question-form, casual, formal) to generate realistic
training data for the NLU intent classifier.
"""

from __future__ import annotations

# fmt: off

NL_TEMPLATES: dict[str, list[str]] = {

    # ── File Operations (12) ───────────────────────────────────────────────

    "find_files": [
        "find all {name_pattern} files in {path}",
        "search for files named {name_pattern} under {path}",
        "locate {type} files matching {name_pattern} in {path}",
        "find files bigger than {size_gt} in {path}",
        "show me files smaller than {size_lt} in {path}",
        "find files in {path} modified in the last {mtime} days",
        "look for {name_pattern} files owned by {user} in {path}",
        "list all files in {path} with permissions {perm}",
        "where are the {name_pattern} files under {path}",
        "can you find {type} files named {name_pattern} in {path}",
        "search {path} for {name_pattern} files larger than {size_gt}",
        "I need to locate {name_pattern} files in {path}",
        "find every {name_pattern} under {path} owned by {user}",
        "please locate files matching {name_pattern} in {path}",
        "hunt down all {name_pattern} in {path} modified within {mtime} days",
        "which files in {path} match {name_pattern} and are bigger than {size_gt}",
    ],

    "copy_files": [
        "copy {source} to {destination}",
        "duplicate {source} into {destination}",
        "cp {source} to {destination}",
        "copy {source} to {destination} recursively",
        "please copy {source} over to {destination}",
        "I want to copy {source} to {destination} and preserve attributes",
        "make a copy of {source} at {destination}",
        "clone {source} into {destination}",
        "copy the contents of {source} to {destination}",
        "replicate {source} to {destination} with {preserve} mode",
        "how do I copy {source} to {destination} recursively",
        "transfer a copy of {source} into {destination}",
        "back up {source} by copying it to {destination}",
        "can you copy {source} to {destination} preserving permissions",
    ],

    "move_files": [
        "move {source} to {destination}",
        "mv {source} to {destination}",
        "relocate {source} to {destination}",
        "rename {source} to {destination}",
        "transfer {source} to {destination}",
        "I need to move {source} into {destination}",
        "please move {source} over to {destination}",
        "shift {source} to {destination}",
        "how do I move {source} to {destination}",
        "put {source} in {destination}",
        "can you move {source} to {destination} for me",
        "take {source} and move it to {destination}",
    ],

    "delete_files": [
        "delete {target}",
        "remove {target}",
        "rm {target}",
        "get rid of {target}",
        "delete {target} recursively",
        "force delete {target}",
        "remove {target} and everything inside it",
        "I want to delete {target}",
        "please remove {target} permanently",
        "wipe out {target}",
        "can you delete {target} for me",
        "how do I remove {target} recursively",
        "trash {target}",
        "erase {target} from the system",
    ],

    "change_permissions": [
        "change permissions of {target} to {permissions}",
        "chmod {permissions} {target}",
        "set {target} permissions to {permissions}",
        "make {target} {permissions}",
        "update the permissions on {target} to {permissions}",
        "change {target} to {permissions} recursively",
        "set permissions {permissions} on {target}",
        "please chmod {target} to {permissions}",
        "I need to change permissions on {target} to {permissions}",
        "modify {target} permissions to {permissions}",
        "can you set {permissions} on {target}",
        "how do I change {target} to {permissions}",
        "recursively set {target} permissions to {permissions}",
        "apply {permissions} to {target}",
    ],

    "change_ownership": [
        "change ownership of {target} to {owner}",
        "chown {owner} {target}",
        "set the owner of {target} to {owner}",
        "change {target} owner to {owner} and group to {group}",
        "make {owner} the owner of {target}",
        "transfer ownership of {target} to {owner}:{group}",
        "chown {owner}:{group} {target} recursively",
        "please change the owner of {target} to {owner}",
        "I need to set ownership of {target} to {owner}",
        "update {target} ownership to {owner}:{group}",
        "can you change the owner of {target} to {owner}",
        "recursively change ownership of {target} to {owner}",
        "assign {target} to user {owner} and group {group}",
    ],

    "create_directory": [
        "create directory {path}",
        "mkdir {path}",
        "make a directory called {path}",
        "create the folder {path}",
        "create {path} with parent directories",
        "mkdir -p {path}",
        "please create directory {path}",
        "I need a new directory at {path}",
        "make the directory {path} and any parents",
        "set up a new folder at {path}",
        "can you create the directory {path}",
        "how do I create {path} and all parent directories",
        "create the directory tree {path}",
    ],

    "list_directory": [
        "list files in {path}",
        "ls {path}",
        "show the contents of {path}",
        "what is in {path}",
        "list {path} including hidden files",
        "show me everything in {path}",
        "list {path} sorted by {sort_by}",
        "display the contents of {path}",
        "please list the files in {path}",
        "ls -la {path}",
        "show all files in {path} with details",
        "can you list what is in {path}",
        "I want to see the files in {path}",
        "list {path} in long format",
    ],

    "disk_usage": [
        "show disk usage for {path}",
        "how much space is {path} using",
        "du {path}",
        "check disk usage of {path}",
        "show me the top {top_n} largest items in {path}",
        "what is the disk usage of {path}",
        "display disk consumption for {path}",
        "how big is {path}",
        "analyze disk usage in {path} to depth {max_depth}",
        "please show disk usage for {path}",
        "I need to check how much space {path} is consuming",
        "show the largest directories in {path}",
        "can you check the disk usage of {path}",
        "list disk usage under {path} sorted by size",
    ],

    "view_file": [
        "show me {file}",
        "view {file}",
        "cat {file}",
        "display the contents of {file}",
        "show the first {lines} lines of {file}",
        "show the last {lines} lines of {file}",
        "tail {file}",
        "head {file}",
        "read {file}",
        "print {file} to the screen",
        "show lines {line_start} to {line_end} of {file}",
        "I want to see the contents of {file}",
        "please show me {file}",
        "display {file} from the end",
        "can you show me {file}",
        "open {file} and display it",
    ],

    "create_symlink": [
        "create a symlink from {link_name} to {target}",
        "ln -s {target} {link_name}",
        "make a symbolic link {link_name} pointing to {target}",
        "link {link_name} to {target}",
        "create a symbolic link to {target} called {link_name}",
        "please create a symlink {link_name} that points to {target}",
        "I need a symlink from {link_name} to {target}",
        "set up a symbolic link {link_name} for {target}",
        "symlink {target} as {link_name}",
        "can you create a link {link_name} pointing at {target}",
        "how do I create a symlink from {link_name} to {target}",
        "make {link_name} a symbolic link to {target}",
    ],

    "compare_files": [
        "compare {file1} and {file2}",
        "diff {file1} {file2}",
        "show the differences between {file1} and {file2}",
        "what is different between {file1} and {file2}",
        "compare {file1} with {file2} side by side",
        "please diff {file1} against {file2}",
        "I need to compare {file1} and {file2}",
        "show me how {file1} differs from {file2}",
        "run a diff on {file1} and {file2}",
        "can you compare {file1} to {file2}",
        "check for differences between {file1} and {file2}",
        "display a side-by-side comparison of {file1} and {file2}",
    ],

    # ── Text Processing (6) ───────────────────────────────────────────────

    "search_text": [
        "search for {pattern} in {path}",
        "grep {pattern} in {path}",
        "find {pattern} in files under {path}",
        "look for {pattern} in {path}",
        "search {path} for the string {pattern}",
        "grep -r {pattern} {path}",
        "find occurrences of {pattern} in {path} recursively",
        "search for {pattern} in {path} ignoring case",
        "where does {pattern} appear in {path}",
        "can you search for {pattern} in {path} with line numbers",
        "I need to find {pattern} in {path}",
        "please search {path} for {pattern}",
        "look for the regex {pattern} in {path}",
        "grep for {pattern} in {path} case-insensitively",
        "show me all lines matching {pattern} in {path}",
    ],

    "replace_text": [
        "replace {pattern} with {replacement} in {file}",
        "substitute {pattern} for {replacement} in {file}",
        "change {pattern} to {replacement} in {file}",
        "sed {pattern} to {replacement} in {file}",
        "find and replace {pattern} with {replacement} in {file}",
        "swap {pattern} for {replacement} in {file}",
        "globally replace {pattern} with {replacement} in {file}",
        "I need to replace {pattern} with {replacement} in {file}",
        "update all occurrences of {pattern} to {replacement} in {file}",
        "please change {pattern} to {replacement} in {file}",
        "can you replace every {pattern} with {replacement} in {file}",
        "modify {file} by replacing {pattern} with {replacement}",
        "in {file} change {pattern} to {replacement}",
    ],

    "sort_output": [
        "sort {file}",
        "sort the contents of {file}",
        "sort {file} numerically",
        "sort {file} in reverse order",
        "sort {file} by column {key}",
        "sort {file} using delimiter {delimiter}",
        "please sort {file}",
        "I need {file} sorted",
        "can you sort {file} by the {key} field",
        "how do I sort {file} numerically in reverse",
        "sort {file} by key {key} with delimiter {delimiter}",
        "organize {file} in sorted order",
        "arrange the lines of {file} alphabetically",
        "reverse sort {file}",
    ],

    "count_lines": [
        "count the lines in {file}",
        "wc {file}",
        "how many lines does {file} have",
        "count words in {file}",
        "count characters in {file}",
        "show the line count for {file}",
        "how many words are in {file}",
        "please count the lines in {file}",
        "I need to know how many lines are in {file}",
        "can you count the lines in {file}",
        "what is the word count of {file}",
        "give me the line count for {file}",
    ],

    "extract_columns": [
        "extract column {columns} from {file}",
        "cut column {columns} from {file}",
        "get field {columns} from {file} using delimiter {delimiter}",
        "show column {columns} of {file}",
        "extract fields {columns} from {file}",
        "pull out column {columns} from {file}",
        "please extract column {columns} from {file} with delimiter {delimiter}",
        "I need column {columns} from {file}",
        "can you extract column {columns} from {file}",
        "awk column {columns} from {file}",
        "show me field {columns} from {file} separated by {delimiter}",
        "cut fields {columns} from {file} using {delimiter}",
    ],

    "unique_lines": [
        "show unique lines in {file}",
        "remove duplicates from {file}",
        "deduplicate {file}",
        "uniq {file}",
        "show unique lines in {file} with counts",
        "filter out duplicate lines from {file}",
        "get distinct lines from {file}",
        "please remove duplicate lines from {file}",
        "I need only the unique lines from {file}",
        "can you deduplicate {file}",
        "list unique entries in {file}",
        "show me duplicates in {file} with their counts",
    ],

    # ── Archive Operations (2) ─────────────────────────────────────────────

    "compress_archive": [
        "compress {source} into {output}",
        "create an archive of {source} as {output}",
        "tar {source} into {output}",
        "zip {source} into {output}",
        "archive {source} to {output}",
        "pack {source} into {output} as {format}",
        "create a {format} archive of {source}",
        "bundle {source} into {output}",
        "please compress {source} into {output}",
        "I need to archive {source} as {output}",
        "can you compress {source} into a {format} archive",
        "how do I create a {format} archive from {source}",
        "make an archive of {source} and save it as {output}",
        "tar and gzip {source} to {output}",
    ],

    "extract_archive": [
        "extract {archive}",
        "unzip {archive}",
        "untar {archive}",
        "extract {archive} to {destination}",
        "decompress {archive} into {destination}",
        "unpack {archive}",
        "please extract {archive} to {destination}",
        "I need to extract {archive}",
        "can you extract {archive} into {destination}",
        "how do I unzip {archive} to {destination}",
        "open the archive {archive} and extract to {destination}",
        "uncompress {archive} to {destination}",
    ],

    # ── Package Management (4) ─────────────────────────────────────────────

    "install_package": [
        "install {package}",
        "install the {package} package",
        "apt install {package}",
        "I need to install {package}",
        "please install {package}",
        "set up {package}",
        "install {package} version {version}",
        "can you install {package} for me",
        "add the {package} package",
        "how do I install {package}",
        "get {package} installed",
        "install {package} without prompting",
        "deploy {package} on this system",
        "I want to install {package} version {version}",
    ],

    "remove_package": [
        "remove {package}",
        "uninstall {package}",
        "apt remove {package}",
        "get rid of {package}",
        "please remove the {package} package",
        "I need to uninstall {package}",
        "can you remove {package}",
        "purge {package} completely",
        "delete the {package} package",
        "take {package} off the system",
        "how do I uninstall {package}",
        "remove {package} and purge its config",
    ],

    "update_packages": [
        "update all packages",
        "run a system update",
        "update the package list",
        "upgrade all installed packages",
        "apt update and upgrade",
        "please update all packages",
        "I need to update the system packages",
        "can you run an update",
        "bring all packages up to date",
        "how do I update everything",
        "perform a full system upgrade",
        "update and upgrade all packages",
    ],

    "search_package": [
        "search for {query}",
        "look for a package called {query}",
        "is there a package for {query}",
        "find a package matching {query}",
        "apt search {query}",
        "search the package repos for {query}",
        "please search for {query} in the package manager",
        "I need to find a package named {query}",
        "can you search for {query}",
        "what packages match {query}",
        "look up {query} in the repositories",
        "check if {query} is available as a package",
    ],

    # ── Service Management (5) ─────────────────────────────────────────────

    "start_service": [
        "start {service}",
        "start the {service} service",
        "systemctl start {service}",
        "bring up {service}",
        "please start {service}",
        "I need to start {service}",
        "can you start {service}",
        "launch {service}",
        "fire up {service}",
        "how do I start {service}",
        "get {service} running",
        "turn on {service}",
    ],

    "stop_service": [
        "stop {service}",
        "stop the {service} service",
        "systemctl stop {service}",
        "shut down {service}",
        "please stop {service}",
        "I need to stop {service}",
        "can you stop {service}",
        "kill the {service} service",
        "bring {service} down",
        "how do I stop {service}",
        "turn off {service}",
        "halt {service}",
    ],

    "restart_service": [
        "restart {service}",
        "restart the {service} service",
        "systemctl restart {service}",
        "bounce {service}",
        "please restart {service}",
        "I need to restart {service}",
        "can you restart {service}",
        "cycle {service}",
        "reload {service} by restarting it",
        "how do I restart {service}",
        "stop and start {service}",
        "bring {service} back up",
    ],

    "enable_service": [
        "enable {service}",
        "enable {service} at boot",
        "systemctl enable {service}",
        "make {service} start on boot",
        "please enable {service}",
        "I want {service} to start automatically",
        "can you enable {service} on boot",
        "set {service} to start at boot time",
        "how do I enable {service}",
        "auto-start {service} on boot",
        "configure {service} to run on startup",
        "add {service} to the startup services",
    ],

    "service_status": [
        "check the status of {service}",
        "is {service} running",
        "systemctl status {service}",
        "show me the status of {service}",
        "what is the status of {service}",
        "please check if {service} is running",
        "I need the status of {service}",
        "can you check on {service}",
        "how is {service} doing",
        "is {service} up",
        "get the status of {service}",
        "tell me if {service} is active",
    ],

    # ── User Management (3) ───────────────────────────────────────────────

    "create_user": [
        "create user {username}",
        "add a new user {username}",
        "useradd {username}",
        "create user {username} with home directory {home_dir}",
        "add user {username} with shell {shell}",
        "please create user {username}",
        "I need to add a user called {username}",
        "create user {username} and add to group {groups}",
        "can you create user {username} with shell {shell}",
        "set up a new user {username} with home {home_dir}",
        "how do I add user {username}",
        "make a new account for {username}",
        "add user {username} to groups {groups}",
    ],

    "delete_user": [
        "delete user {username}",
        "remove user {username}",
        "userdel {username}",
        "delete user {username} and their home directory",
        "please remove user {username}",
        "I need to delete the user {username}",
        "can you remove user {username}",
        "get rid of user {username}",
        "how do I delete user {username}",
        "remove the account for {username}",
        "drop user {username} and remove their home",
        "purge user {username}",
    ],

    "modify_user": [
        "modify user {username}",
        "change the shell for {username} to {shell}",
        "add {username} to group {groups}",
        "update user {username} groups to {groups}",
        "usermod {username}",
        "please modify user {username}",
        "change {username} home directory to {home_dir}",
        "I need to modify user {username}",
        "can you change the shell for {username} to {shell}",
        "set {username} shell to {shell}",
        "how do I add {username} to group {groups}",
        "update {username} settings",
        "move {username} home to {home_dir}",
    ],

    # ── Log Operations (3) ─────────────────────────────────────────────────

    "view_logs": [
        "show logs for {service}",
        "view {service} logs",
        "journalctl -u {service}",
        "show the last {lines} lines of {service} logs",
        "display logs for {service}",
        "please show me the {service} logs",
        "I need to see the logs for {service}",
        "can you show {service} logs",
        "what do the {service} logs say",
        "show {service} logs since {since}",
        "how do I view {service} logs",
        "pull up the logs for {service}",
        "get me the last {lines} log entries for {service}",
    ],

    "follow_logs": [
        "follow {service} logs",
        "tail -f the logs for {service}",
        "stream {service} logs",
        "watch {service} logs in real time",
        "show me live logs for {service}",
        "please follow the {service} logs",
        "I want to tail {service} logs",
        "can you stream {service} logs",
        "monitor {service} logs live",
        "how do I follow {service} logs",
        "attach to {service} log output",
        "keep watching {service} logs",
    ],

    "filter_logs": [
        "filter {service} logs for {pattern}",
        "search {service} logs for {pattern}",
        "grep {service} logs for {pattern}",
        "show {service} log entries matching {pattern}",
        "find {pattern} in {service} logs",
        "filter {service} logs since {since} for {pattern}",
        "please filter {service} logs by {pattern}",
        "I need to find {pattern} in the {service} logs",
        "can you search {service} logs for {pattern}",
        "look for {pattern} in {service} logs since {since}",
        "show me {service} log lines containing {pattern}",
        "which {service} log entries match {pattern}",
    ],

    # ── Scheduling (3) ─────────────────────────────────────────────────────

    "schedule_cron": [
        "schedule {command} to run at {schedule}",
        "create a cron job for {command} at {schedule}",
        "add a cron entry: {schedule} {command}",
        "set up a cron job to run {command} on schedule {schedule}",
        "please schedule {command} with cron at {schedule}",
        "I need to schedule {command} to run at {schedule}",
        "can you create a cron job for {command}",
        "add {command} to crontab with schedule {schedule}",
        "how do I schedule {command} to run at {schedule}",
        "cron job: run {command} at {schedule}",
        "set up a recurring task: {command} at {schedule}",
        "automate {command} on schedule {schedule}",
    ],

    "list_cron": [
        "list all cron jobs",
        "show my crontab",
        "crontab -l",
        "display all scheduled cron jobs",
        "what cron jobs are set up",
        "please list the cron jobs",
        "I need to see the crontab",
        "can you show me the cron jobs",
        "show all scheduled tasks",
        "how do I list cron jobs",
        "what is in my crontab",
        "display the current crontab",
    ],

    "remove_cron": [
        "remove the cron job matching {pattern}",
        "delete the cron job for {pattern}",
        "remove the crontab entry for {pattern}",
        "get rid of the cron job {pattern}",
        "please remove the cron entry matching {pattern}",
        "I need to delete the cron job for {pattern}",
        "can you remove the {pattern} cron job",
        "how do I remove a cron job for {pattern}",
        "drop the cron entry for {pattern}",
        "unschedule {pattern} from cron",
        "clear the {pattern} cron task",
        "take {pattern} out of the crontab",
    ],

    # ── Networking (6) ─────────────────────────────────────────────────────

    "network_info": [
        "show network information",
        "display network interfaces",
        "ifconfig",
        "show the IP address of {interface}",
        "what is my IP address",
        "show network info for {interface}",
        "please show network configuration",
        "I need to see the network interfaces",
        "can you show the network info for {interface}",
        "how do I check my IP address",
        "display the network settings for {interface}",
        "list all network interfaces",
        "show me the network configuration",
    ],

    "test_connectivity": [
        "ping {host}",
        "test connectivity to {host}",
        "check if {host} is reachable",
        "ping {host} {count} times",
        "can I reach {host}",
        "please ping {host}",
        "I need to check connectivity to {host}",
        "is {host} responding",
        "test the connection to {host}",
        "how do I test if {host} is up",
        "send {count} pings to {host}",
        "verify {host} is online",
    ],

    "download_file": [
        "download {url}",
        "wget {url}",
        "curl {url}",
        "download {url} and save as {output}",
        "fetch {url}",
        "please download {url}",
        "I need to download {url}",
        "grab {url} and save it to {output}",
        "can you download {url}",
        "how do I download {url}",
        "pull down {url} to {output}",
        "save {url} as {output}",
        "get {url} and store it as {output}",
    ],

    "transfer_file": [
        "transfer {source} to {host}:{destination}",
        "scp {source} to {user}@{host}:{destination}",
        "copy {source} to {host}",
        "send {source} to {user}@{host}:{destination}",
        "upload {source} to {host}:{destination}",
        "please transfer {source} to {host}",
        "I need to copy {source} to {user}@{host}:{destination}",
        "can you scp {source} to {host}:{destination}",
        "how do I transfer {source} to {host}",
        "push {source} to {user}@{host}:{destination}",
        "rsync {source} to {host}:{destination}",
        "move {source} over to {user}@{host}:{destination}",
    ],

    "ssh_connect": [
        "ssh into {host}",
        "connect to {host} via ssh",
        "ssh {user}@{host}",
        "open an ssh session to {host}",
        "ssh to {host} as {user}",
        "please ssh into {host}",
        "I need to connect to {host} over ssh",
        "can you ssh to {user}@{host}",
        "log in to {host} via ssh on port {port}",
        "how do I ssh into {host}",
        "ssh to {host} and run {command}",
        "connect to {user}@{host} on port {port}",
        "remote into {host} as {user}",
        "run {command} on {host} via ssh",
    ],

    "port_check": [
        "check if port {port} is open on {host}",
        "is port {port} open on {host}",
        "test port {port} on {host}",
        "scan port {port} on {host}",
        "check port {port} on {host}",
        "please check if port {port} is listening on {host}",
        "I need to verify port {port} on {host}",
        "can you check port {port} on {host}",
        "how do I test if port {port} is open on {host}",
        "is {host} listening on port {port}",
        "probe port {port} on {host}",
        "verify port {port} connectivity to {host}",
    ],

    # ── Process Management (3) ─────────────────────────────────────────────

    "process_list": [
        "list all processes",
        "show running processes",
        "ps aux",
        "list processes for user {user}",
        "show processes matching {filter}",
        "what processes are running",
        "please list all processes",
        "I need to see the running processes",
        "can you show the process list",
        "how do I list processes for {user}",
        "display all running processes",
        "which processes match {filter}",
        "show me the processes owned by {user}",
    ],

    "kill_process": [
        "kill process {pid}",
        "terminate process {pid}",
        "kill {name}",
        "send signal {signal} to process {pid}",
        "stop process {pid}",
        "kill all {name} processes",
        "please kill process {pid}",
        "I need to kill process {pid}",
        "can you terminate {name}",
        "how do I kill process {pid}",
        "force kill process {pid}",
        "send {signal} to {pid}",
        "end the {name} process",
    ],

    "system_info": [
        "show system information",
        "show {component} usage",
        "how much memory is being used",
        "check CPU usage",
        "show disk usage",
        "what is the system uptime",
        "display {component} info",
        "please show {component} information",
        "I need to check {component} usage",
        "can you show me the system stats",
        "how is the {component} doing",
        "give me a system overview",
        "check the {component} status",
        "show me current {component} utilization",
    ],

    # ── Disk/Mount (2) ─────────────────────────────────────────────────────

    "mount_device": [
        "mount {device} at {mount_point}",
        "mount {device} to {mount_point}",
        "mount {device} as {filesystem} at {mount_point}",
        "please mount {device} on {mount_point}",
        "I need to mount {device} at {mount_point}",
        "can you mount {device} to {mount_point}",
        "how do I mount {device} at {mount_point}",
        "attach {device} to {mount_point}",
        "mount {device} on {mount_point} with filesystem {filesystem}",
        "bring up {device} at {mount_point}",
        "mount the drive {device} at {mount_point}",
        "set up {device} at {mount_point} as {filesystem}",
    ],

    "unmount_device": [
        "unmount {mount_point}",
        "umount {mount_point}",
        "detach {mount_point}",
        "please unmount {mount_point}",
        "I need to unmount {mount_point}",
        "can you unmount {mount_point}",
        "how do I unmount {mount_point}",
        "eject {mount_point}",
        "safely remove {mount_point}",
        "take down {mount_point}",
        "disconnect {mount_point}",
        "release the mount at {mount_point}",
    ],
}

# fmt: on
